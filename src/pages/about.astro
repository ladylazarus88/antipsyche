---
import Layout from '../components/Layout.astro';
import { config } from '../config';

const { title, description, author, social } = config;
const pageDescription = `Scopri di più su ${author.name} e il blog ${title}`;
const base = import.meta.env.BASE_URL;

// Normalizza il percorso dell'avatar
const getAvatarPath = () => {
  if (author.avatar) {
    // Se author.avatar è definito, usalo
    return author.avatar.startsWith('http') ? author.avatar : `${base}${author.avatar.replace(/^\//, '')}`;
  }
  // Altrimenti usa il percorso di default
  return `${base}images/avatar.png`;
};

const avatarPath = getAvatarPath();
---

<Layout title={`Chi Siamo | ${title}`} description={pageDescription}>
  <div class="about-page page-container">
    <header class="page-header">
      <h1 class="page-title">Chi siamo</h1>
      <p class="page-description">INFORMAZIONI SUL PROGETTO</p>
      <hr class="page-rule" />
    </header>

    <div class="content-section">
      <h2>Informazioni sul Blog</h2>
      <p>{description}</p>
      
      <h2>Filosofia del Progetto</h2>
      <p>
        Antipsyché nasce dalla volontà di esplorare il territorio dei testi dimenticati 
        e delle idee trascurate. Uniamo rigore filologico e passione curatoriale per costruire 
        ponti tra voci del passato e lettori contemporanei, attraverso traduzioni inedite 
        e analisi critiche che danno nuova vita a pensieri aporetici.
      </p>

      <h2>Principi Fondamentali</h2>
      <ul>
        <li><strong>Testi Inediti</strong>: Ci concentriamo esclusivamente su opere mai pubblicate in italiano.</li>
        <li><strong>Traduzioni Artigianali</strong>: Un approccio meticoloso che unisce fedeltà al testo e fluidità espressiva.</li>
        <li><strong>Analisi Critica</strong>: Ogni testo è accompagnato da un commento che ne illumina il contesto e le implicazioni.</li>
        <li><strong>Accessibilità</strong>: Rendere disponibili gratuitamente opere altrimenti inaccessibili.</li>
      </ul>
    </div>
  </div>
</Layout>

<style is:global>
  /* Layout principale */
  .about-page {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  /* Header */
  .page-header {
    text-align: center;
    margin-bottom: 3rem;
    padding: 0 1rem;
  }

  .page-title {
    font-family: var(--font-heading-primary);
    font-size: clamp(2rem, 5vw, 2.5rem);
    font-weight: 300;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
    line-height: 1.1;
  }

  .page-description {
    font-family: var(--font-heading-secondary);
    font-size: 1rem;
    font-weight: 400;
    line-height: 1.5;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 1.5rem;
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
  }

  .page-rule {
    height: 2px;
    background-color: var(--color-accent);
    border: none;
    width: 80px;
    margin: 0 auto;
  }

  /* Sezione autore */
  .author-section {
    text-align: center;
    margin-bottom: 4rem;
    padding: 3rem 2rem;
    background-color: var(--color-surface);
    border-radius: 1rem;
    border: 1px solid var(--color-border);
    box-shadow: var(--shadow-sm);
  }

  /* Container avatar - centrato perfettamente */
  .author-avatar-container {
    display: flex;
    justify-content: center;
    margin-bottom: 2rem;
  }

  .author-avatar {
    width: 160px;
    height: 160px;
    margin: 0 auto;
    position: relative;
  }

  /* Immagine avatar - perfettamente centrata e responsiva */
  .author-avatar-img {
    width: 100% !important;
    height: 100% !important;
    border-radius: 50%;
    object-fit: cover;
    border: 4px solid var(--color-accent);
    display: block;
    margin: 0 auto;
    min-width: 80px;
    min-height: 80px;
    max-width: 160px;
    max-height: 160px;
  }

  /* Forza il caricamento dell'immagine su tutti i dispositivi */
  .author-avatar-img {
    content-visibility: visible !important;
    opacity: 1 !important;
    visibility: visible !important;
  }

  .author-name {
    font-family: var(--font-heading-secondary);
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
  }

  .author-bio {
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto 2rem;
  }

  .social-links {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    align-items: center;
  }

  .social-links a {
    color: var(--color-text-secondary);
    transition: color 0.2s ease, transform 0.2s ease;
  }

  .social-links a:hover {
    color: var(--color-accent);
    transform: translateY(-2px);
  }

  .social-icon {
    width: 24px;
    height: 24px;
  }

  /* Sezione contenuto */
  .content-section {
    line-height: 1.7;
  }

  .content-section h2 {
    font-family: var(--font-heading-secondary);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-top: 3rem;
    margin-bottom: 1.5rem;
  }

  .content-section p {
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--color-text-primary);
    margin-bottom: 1.5rem;
  }

  .content-section ul {
    margin: 1.5rem 0;
    padding-left: 1.5rem;
  }

  .content-section li {
    margin-bottom: 1rem;
    font-size: 1.125rem;
    line-height: 1.6;
  }

  .content-section strong {
    color: var(--color-text-primary);
  }

  /* ===== RESPONSIVE DESIGN ===== */
  @media (max-width: 768px) {
    .about-page {
      padding: 1rem;
    }
    
    .author-section {
      padding: 2rem 1.5rem;
      margin-bottom: 3rem;
    }

    .author-avatar {
      width: 120px;
      height: 120px;
    }

    .author-avatar-img {
      width: 120px !important;
      height: 120px !important;
      min-width: 80px;
      min-height: 80px;
    }

    .author-name {
      font-size: 1.75rem;
    }

    .author-bio {
      font-size: 1rem;
    }

    .social-links {
      gap: 1.25rem;
    }

    .social-icon {
      width: 22px;
      height: 22px;
    }

    .content-section h2 {
      font-size: 1.375rem;
    }

    .content-section p,
    .content-section li {
      font-size: 1rem;
    }
  }

  @media (max-width: 480px) {
    .author-section {
      padding: 1.5rem 1rem;
    }

    .author-avatar {
      width: 100px;
      height: 100px;
    }

    .author-avatar-img {
      width: 100px !important;
      height: 100px !important;
    }

    .author-name {
      font-size: 1.5rem;
    }

    .social-links {
      gap: 1rem;
    }

    .social-icon {
      width: 20px;
      height: 20px;
    }
  }

  /* Stile per debug - rimuovi in produzione */
  .author-avatar-img:not([src]),
  .author-avatar-img[src=""] {
    border: 3px dashed red !important;
    background-color: rgba(255, 0, 0, 0.1);
  }
</style>

<script>
  // Script per debug avatar
  document.addEventListener('DOMContentLoaded', () => {
    const avatarImg = document.querySelector('.author-avatar-img');
    if (avatarImg) {
      console.log('Avatar src:', avatarImg.src);
      
      // Verifica se l'immagine è stata caricata
      if (avatarImg.complete) {
        if (avatarImg.naturalWidth === 0 || avatarImg.naturalHeight === 0) {
          console.warn('Avatar non caricato:', avatarImg.src);
        } else {
          console.log('Avatar caricato correttamente:', avatarImg.naturalWidth, 'x', avatarImg.naturalHeight);
        }
      }
    }
  });
</script>
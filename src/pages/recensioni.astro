---
import Layout from '../components/Layout.astro';
const base = import.meta.env.BASE_URL;

// CONFIGURAZIONE UNIFICATA
const CONFIG = {
  rowHeight: '360px',
  gap: '1.5rem',
  palettes: [
    { bg: '#FFF0F5', text: '#333', accent: '#8B5F65' },
    { bg: '#F0F8FF', text: '#333', accent: '#5C7080' },
    { bg: '#FFF5EE', text: '#333', accent: '#8B7355' },
    { bg: '#F5F0FF', text: '#333', accent: '#6A5F7C' }
  ],
  layouts: [
    ['text', 'image', 'image'],
    ['image', 'text', 'image'],
    ['image', 'image', 'text']
  ]
};

// FUNZIONE UNICA PER LE RECENSIONI
const getReviewPosts = () => {
  const posts = import.meta.glob(['../content/blog/*.md', '../content/blog/*.mdx'], { 
    eager: true 
  });
  
  return Object.values(posts)
    .filter(post => post.frontmatter.categories?.includes('Recensioni'))
    .map(post => {
      const slug = post.file.split('/').pop()?.replace(/\.(md|mdx)$/, '');
      const title = post.frontmatter.title?.replace(/^Recensione:\s*/i, '');
      
      return {
        slug,
        title,
        url: `${base}blog/${slug}`,
        author: post.frontmatter.author || 'Autore',
        excerpt: post.frontmatter.excerpt?.substring(0, 120) || 'Leggi la recensione completa...',
        mainImage: post.frontmatter.image,
        altImage: post.frontmatter.image_alt,
        hasAltImage: !!post.frontmatter.image_alt
      };
    });
};

const reviews = getReviewPosts();
---

<Layout title="Recensioni | Antipsyché">
  <div class="reviews-page">
    <header class="page-header">
      <h1 class="page-title">Recensioni</h1>
      <p class="page-description">Letture quotidiane.</p>
      <hr class="page-rule" />
    </header>

    <main class="reviews-grid">
      {reviews.map((review, index) => {
        const paletteIndex = index % CONFIG.palettes.length;
        const layout = CONFIG.layouts[index % CONFIG.layouts.length];
        let imageIndex = 0;
        
        return (
          <article 
            class="review-row" 
            data-palette-index={paletteIndex}
            style={`
              --row-height: ${CONFIG.rowHeight};
              --row-gap: ${CONFIG.gap};
            `}
          >
            <a href={review.url} class="review-link">
              {layout.map((tileType) => {
                if (tileType === 'text') {
                  return (
                    <div class="tile text-tile">
                      <div class="tile-content">
                        <div class="text-content">
                          <span class="book-author">{review.author}</span>
                          <h3 class="book-title">{review.title}</h3>
                          <p class="book-excerpt">{review.excerpt}</p>
                          <span class="read-more">Leggi →</span>
                        </div>
                      </div>
                    </div>
                  );
                }
                
                // Tile immagine
                const isFirstImage = imageIndex === 0;
                imageIndex++;
                const imageSrc = isFirstImage ? review.mainImage : 
                                (review.hasAltImage ? review.altImage : review.mainImage);
                const imageAlt = isFirstImage ? `Copertina: ${review.title}` : `Dettaglio: ${review.title}`;
                
                return (
                  <div class="tile image-tile">
                    <div class="tile-content">
                      {imageSrc ? (
                        <img 
                          src={imageSrc} 
                          alt={imageAlt}
                          loading="lazy"
                          class={review.hasAltImage && !isFirstImage ? 'secondary-image' : ''}
                        />
                      ) : (
                        <div class="image-placeholder">
                          {isFirstImage ? 'Copertina' : 'Dettaglio'}
                        </div>
                      )}
                    </div>
                  </div>
                );
              })}
            </a>
          </article>
        );
      })}
    </main>
  </div>
</Layout>

<style is:global>
  /* ===== VARIABILI CSS ===== */
  :root {
    --page-bg: #f8f9fa;
    --row-border: rgba(0, 0, 0, 0.08);
    --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.08);
    --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.12);
  }
  
  /* ===== PALETTE COLORS ===== */
  .review-row[data-palette-index="0"] {
    --row-bg: #FFF0F5;
    --row-text: #333;
    --row-accent: #8B5F65;
  }
  
  .review-row[data-palette-index="1"] {
    --row-bg: #F0F8FF;
    --row-text: #333;
    --row-accent: #5C7080;
  }
  
  .review-row[data-palette-index="2"] {
    --row-bg: #FFF5EE;
    --row-text: #333;
    --row-accent: #8B7355;
  }
  
  .review-row[data-palette-index="3"] {
    --row-bg: #F5F0FF;
    --row-text: #333;
    --row-accent: #6A5F7C;
  }

  /* ===== LAYOUT BASE ===== */
  .reviews-page {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    background: var(--page-bg);
    min-height: 100vh;
  }

  /* ===== HEADER ===== */
  .page-header {
    text-align: center;
    margin-bottom: 3rem;
    padding: 0 1rem;
  }

  .page-title {
    font-family: var(--font-heading-primary);
    font-size: clamp(2rem, 5vw, 2.5rem);
    font-weight: 50;
    color: #2c3e50;
    margin-bottom: 0.5rem;
    line-height: 1.1;
  }

  .page-description {
    font-size: 1rem;
    line-height: 1.5;
    color: #7f8c8d;
    margin-bottom: 1.5rem;
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
  }

  .page-rule {
    height: 2px;
    background-color: #8B5F65;
    border: none;
    width: 80px;
    margin: 0 auto;
  }

  /* ===== GRIGLIA PRINCIPALE ===== */
  .reviews-grid {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* ===== RIGA SINGOLA ===== */
  .review-row {
    background: var(--row-bg);
    border-radius: 12px;
    box-shadow: var(--shadow-md);
    border: 1px solid var(--row-border);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    height: var(--row-height);
    overflow: hidden;
  }

  .review-row:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  /* ===== LINK DELLA RIGA ===== */
  .review-link {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--row-gap);
    height: 100%;
    padding: var(--row-gap);
    text-decoration: none;
    color: inherit;
  }

  /* ===== TILE GENERICO ===== */
  .tile {
    border-radius: 8px;
    overflow: hidden;
    background: white;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    transition: box-shadow 0.3s ease;
  }

  /* ===== TILE TESTO ===== */
  .text-tile .tile-content {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }

  .text-content {
    width: 100%;
    max-width: 90%;
  }

  .book-author {
    display: block;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.8rem;
    color: var(--row-accent);
    font-weight: 500;
  }

  .book-title {
    font-family: var(--font-heading-primary);
    font-size: 1.5rem;
    font-weight: 400;
    line-height: 1.2;
    margin-bottom: 1rem;
    color: var(--row-text);
    word-break: break-word;
  }

  .book-excerpt {
    font-size: 0.95rem;
    line-height: 1.5;
    margin-bottom: 1.5rem;
    color: color-mix(in srgb, var(--row-text) 70%, transparent);
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .read-more {
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    color: var(--row-accent);
    display: inline-block;
    transition: transform 0.2s ease;
  }

  .review-row:hover .read-more {
    transform: translateX(4px);
  }

  /* ===== TILE IMMAGINE ===== */
  .image-tile {
    display: flex;
    align-items: center;
    justify-content: center;
    background: white;
  }

  .image-tile img {
    width: auto;
    height: auto;
    max-width: 95%;
    max-height: 95%;
    object-fit: contain;
    transition: transform 0.4s ease;
  }

  .image-tile:hover img {
    transform: scale(1.08);
  }

  .secondary-image {
    filter: grayscale(20%);
    opacity: 0.9;
  }

  .image-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
    color: var(--row-accent);
    background: color-mix(in srgb, var(--row-accent) 10%, white);
    font-weight: 300;
    letter-spacing: 0.05em;
    text-align: center;
  }

  /* ===== RESPONSIVE DESIGN ===== */
  @media (max-width: 768px) {
    .reviews-page {
      padding: 1rem;
    }
    
    .review-row {
      height: auto;
    }
    
    .review-link {
      grid-template-columns: 1fr;
      grid-template-rows: repeat(3, 280px);
      gap: 1rem;
      padding: 1rem;
    }
    
    .text-tile .tile-content {
      padding: 1.5rem;
    }
    
    .book-title {
      font-size: 1.3rem;
    }
  }

  @media (max-width: 480px) {
    .review-link {
      grid-template-rows: repeat(3, 240px);
    }
    
    .book-title {
      font-size: 1.1rem;
    }
    
    .book-excerpt {
      font-size: 0.9rem;
      -webkit-line-clamp: 2;
    }
  }

  @media (min-width: 1400px) {
    .reviews-page {
      max-width: 1400px;
    }
    
    .review-row {
      --row-height: 400px;
    }
  }
</style>